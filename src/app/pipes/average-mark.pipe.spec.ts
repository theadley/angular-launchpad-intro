import { AverageMarkPipe } from './average-mark.pipe';

describe('AverageMarkPipe', () => {
  it('create an instance', () => {
    const pipe = new AverageMarkPipe();
    expect(pipe).toBeTruthy();
  });
});
