import { FilterSeasonsPipe } from './filter-seasons.pipe';

describe('FilterSeasonsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterSeasonsPipe();
    expect(pipe).toBeTruthy();
  });
});
